{
  "name": "manimekalai-the-first-handful",
  "private": true,
  "version": "0.2.0",
  "description": "G2 Prototype 0.2 — 'Human First'. A browser-playable Manimekalai narrative-adventure prototype.",
  "type": "module",
  "license": "SEE ../docs/12-game-adaptation/13-rights-and-provenance.md (Option B — fully reserved interim)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vitest": "^2.1.4"
  }
}
